dist: trusty
language: python
python:
    - "3.5"
    - "3.6"
install: 
    - pip install -r requirements.txt
    - pip install -r requirements-dev.txt
script:
    - py.test --cov=onnxmltools --cov-report=term --cov-report=html --cov-report=xml tests

